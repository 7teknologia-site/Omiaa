import React from 'react';
import { AdminSettings } from './AdminSettings';

export const StoreSettings: React.FC = (props) => {
  return <AdminSettings {...props} />;
};

export default StoreSettings;
